query ActiveOrder {
    activeOrder {
        ...ActiveOrder
        shippingAddress {
            fullName
            streetLine1
            city
            postalCode
            countryCode
            phoneNumber
        }
    }
}
